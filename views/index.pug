head
    <script src="https://cdn.socket.io/3.1.3/socket.io.min.js" integrity="sha384-cPwlPLvBTa3sKAgddT6krw0cJat7egBga3DJepJyrLl4Q9/5WLra3rrnMcyTyOnh" crossorigin="anonymous"></script>

ul#chat
    each val, index in messages
        li
            div
                p= val.user
                p= val.createdAt
                p= val.body

input(type="text", id="userbox", placeholder="escribe tu nombre de usuario...")
input(type="text", id="chatbox", placeholder="escribe lo que quieras...")
button(onClick="sendMessage()") Enviar

script
    include js/main.js